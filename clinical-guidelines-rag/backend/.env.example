# Optional: enables LLM-generated answers via the Anthropic API.
# Without this set, /api/query still retrieves and returns the most relevant
# source passages, just without an LLM-composed synthesis.
ANTHROPIC_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-5
EMBEDDING_MODEL=all-MiniLM-L6-v2
