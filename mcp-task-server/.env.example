# "memory" (default) or "dynamodb"
STORAGE_BACKEND=memory

# Only used when STORAGE_BACKEND=dynamodb
TASKS_TABLE_NAME=mcp-tasks
AWS_REGION=us-east-1
# AWS credentials are picked up from the standard AWS SDK credential chain
# (env vars, shared config file, or an IAM role) — not set here.
